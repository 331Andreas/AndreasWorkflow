# 诉讼案件材料管理系统

**工作风格：**
- 直接执行，先完成后报告
- 遇到 skill 先调用
- 每步完成后暂停确认，待用户确认后再继续（连续执行除外）

**行为约束：**
- 修改本文件和 SKILL.md 须经用户确认
- 外部操作（发邮件、发布等）先确认
- 用户确认前不动文件（删除、移动、覆盖）
- 不自行新建文件夹结构，等用户确认后再创建

**首次使用**：阅读 `SETUP.md`，完成环境检查。（每次开工会由 project-init skill 自动执行）

---

## 一、核心纪律

### 🔴 P0（绝对禁止）

**事实必须对应证据**
- 所有事实认定须严格对应素材包中的证据名称及页码
- 不得以当事人陈述作为事实依据
- 涉及钱款数额的，必须找到数额来源并标注证据名称及页码

**法律法规引用**
- 第一优先从本地 `法律法规库/` 引用，注明文件名及条文编号
- 本地库没有的法条：立即暂停，向用户提示，**严禁**以训练数据替代或自行填充

**不得编造**
- 不得编造或引用未经核实的法条、案例、判例
- 所有引用必须有据可查

**案件概览属性更新**
- 解析出的信息必须写入案件概览 YAML frontmatter
- 绝不删除或清空现有字段值，不确定的字段先展示给用户确认

**.doc 文件处理**
- 遇到 `.doc` 格式须先转为 `.docx` 再提取内容（详见 legal-document-generator skill）

### 🟡 P1（强烈建议）

- 非定稿 → Markdown (.md)；定稿 → .docx
- 命名规则：`{文件名称}_{备注版本}_{编辑日期}.md`
- 每步暂停确认（除非用户明确指示连续执行）

---

## 二、Skill 索引

| Skill | 触发条件 | 职责 |
|---|---|---|
| **project-init** | 每次会话开始 | 读规范+读模板+环境检查 |
| **litigation-analyst** | 案件管理相关 | 三阶段工作流、案件概览格式、文件夹结构、.base写法、Dataview备忘 |
| **markitdown** | 收料/转换文件/OCR | 任意文件→Markdown |
| **legal-document-generator** | 起草文书 | 8类文书生成、命名规范、.doc处理、法条引用 |
| **legal-litigation-viz** | 可视化/时间轴 | Mermaid四图、HTML时间轴、A4打印 |

---

## 三、Obsidian .base 铁律

`.base` 文件用 `order` 定义列（不是 `properties`），用 `file.basename.containsAny` 筛选。
**完整语法详见 litigation-analyst skill**。
